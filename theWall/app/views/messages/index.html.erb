<!doctype html>
<html lang="en">
  <head>
    <title>Title</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  </head>
  <body>
      <h6>CodingDojo Wall</h6>
      <h4>Welcome <%= @user.name%></h4>
      <%= link_to "Log Out", '/sessions/destroy', method:"delete" %>

      <% if flash[:alert] %>
      <h4>notifications</h4>
      <ul>
         <% flash[:alert].each do |error| %>
         <li>
            <%= error %>
         </li>
         <% end %>
      </ul>
      <% end %>
      <form action="/messages/create" method="POST">
         <textarea name="messages[message]" cols="60" rows="4"></textarea>
         <%= button_to "Post Message", '/messages/create', method: :POST %>
      </form>
      <ul>
         <% @messages.each do |msg| %>
         <li>
            <%= msg.user.name %> || <%= msg.message %> <br>

            <ul>
               <% @comments.each do |cmt|%>
                  <% if cmt.message.id == msg.id %>
                     <li>
                        <%= cmt.comment %> || <%= cmt.user.name %>
                     </li>
                  <% end %>
               <% end %>
                  <form action="/comments/create" method="POST">
                     <input type="hidden" name="authenticity_tsoken" value="<%= form_authenticity_token %>">
                     <textarea name="cmt[comment]" cols="60" rows="2"></textarea>
                     <%= button_to "Post Comment", comments_create_path, method: :POST, params: { id: msg.id } %>
                  </form>
            </ul>
         </li>
         <% end %>
      </ul>
    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
  </body>
</html>